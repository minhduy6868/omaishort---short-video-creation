---
applyTo: "apps/web/**/*.{ts,tsx,css}"
---

Read `.cursor/rules/frontend.mdc` and `.cursor/skills/frontend-ui/SKILL.md`.

One studio page. Poll jobs; never label Ken Burns as I2V. After edits: `npx tsc --noEmit` in `apps/web`.
